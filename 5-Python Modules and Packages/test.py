from package.maths import *
from package.subpackage.div import division

print(addition(2, 3))  # This will import the addition function from the package.maths module and add 2 and 3
print(subtraction(5, 2))  # This will import the subtraction function from the package.maths module and subtract 2 from 5
print(multiplication(3, 4))  # This will import the multiplication function from the package.maths module and
print(division(10, 2))  # This will import the division function from the package.subpackage.div module and divide 10 by 2